def sum_of_list(list_a):
    each_length = len(list_a)
    if each_length == 1:
        return list_a[0]
    return list_a[0] + sum_of_list(list_a[1:])
list_a = [2, 5, 1, 7]
sol = sum_of_list(list_a)
print(sol)
